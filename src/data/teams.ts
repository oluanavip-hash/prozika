// Este arquivo não é mais necessário pois os dados estão no Supabase
// Mantido para compatibilidade, mas os dados vêm da API

export interface TeamData {
  id: number;
  name: string;
  league_id: number;
  image1: string;
  image2: string;
  price: number;
}

// Os dados agora vêm diretamente do Supabase
// Este arquivo pode ser removido no futuro
